import { DataType } from "./DataType.js";
import { Formatter as FormatterType } from "./interfaces/Formatter.js";
export declare class Formatter implements FormatterType {
    private plen;
    private value;
    private fields;
    private pattern$;
    private placeholder$;
    private predefined;
    private datetokens;
    private datatype$;
    private tokens;
    size(): number;
    get datatype(): DataType;
    set datatype(datatype: DataType);
    get placeholder(): string;
    get format(): string;
    set format(pattern: string);
    isNull(): boolean;
    last(): number;
    first(): number;
    modifiable(pos: number): boolean;
    getValue(): string;
    setValue(value: string): boolean;
    insCharacter(pos: number, c: string): boolean;
    setCharacter(pos: number, c: string): boolean;
    delete(fr: number, to: number): string;
    finish(): string;
    prev(from: number): number;
    next(from: number): number;
    private isValid;
    private validity;
    private input;
    private setBlank;
    private findField;
    getstring(fr: number, to: number): string;
    setstring(pos: number, value: string): void;
    private replace;
    private finishDate;
    private validateDateField;
    private parseFieldDefinition;
    private escaped;
}
